run = "npm run dev"
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[deployment]
deploymentTarget = "static"
publicDir = "dist"
buildCommand = "npm run build"

[[deployment.rules]]
staticPath = "/"
urlPath = "/"

[[deployment.rules]]
staticPath = "/assets"
urlPath = "/assets"

[[deployment.rewrites]]
from = "/*"
to = "/index.html"